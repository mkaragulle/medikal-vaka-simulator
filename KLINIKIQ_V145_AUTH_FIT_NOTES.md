# KlinikIQ v145 — Auth fit + tag cleanup

- Removed the small auth marketing tags: "Klinik vaka simülasyonu", "Vaka bazlı pratik", "Net performans takibi", "Güvenli demo", and "5 vaka hızlı başlangıç".
- Kept the main demo CTA button intact.
- Forced the desktop auth screen into a single viewport with no page/card scrollbar.
- Aligned the left hero container and right auth container to the same visible height on desktop.
- Tightened right-panel vertical spacing so login and register states fit more safely.
- Slightly increased the existing orbital animation speed without changing the visual structure.
- Preserved the dark-theme variable system and added changes through scoped auth overrides only.
