# KlinikIQ V142 — Repeat Center Tag Cleanup

- Removed the visible `Tekrar Merkezi` kicker from the material repeat page header.
- Removed scope/count chips such as `Bu materyal 0` and `Tüm materyaller 0` from the repeat center UI.
- Removed summary count tags such as `0 yanlış soru`, `0 tekrar kartı`, `0 favori`, and total-item counters.
- Removed small panel count badges to keep the screen cleaner and less tag-heavy.
- Preserved the repeat panels and existing material-opening behavior.
- Build check: `npm run build` completed successfully before packaging.
