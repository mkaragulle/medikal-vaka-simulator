# KlinikIQ V71 — Mode Selector Alignment and Learning Icon Refresh

## Changes
- Replaced the old `BookOpen` icon used next to `Öğrenme` with the cleaner `Lightbulb` icon in the global top bar and TUS mode selector.
- Rebuilt the TUS `Öğrenme / Sınav` segmented selector alignment so both inner buttons are centered and evenly fitted inside the white outer pill.
- Added final CSS overrides to prevent clipping and preserve consistent button height, icon size, spacing, and active-state alignment.

## Preserved
- Existing TUS/Komite navigation handlers.
- Existing study/exam mode state logic.
- Existing AI, scoring, review, and routing logic.
