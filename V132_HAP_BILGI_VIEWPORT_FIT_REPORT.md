# V132 Hap Bilgi Çalış viewport fit refinement

- The flashcard stage was adjusted so the decision dock stays visible in the first desktop viewport.
- The flashcard card is anchored slightly higher and its height now respects the available stage height instead of pushing the bottom dock out of view.
- The right control column rows were restored to a more comfortable height while keeping the whole panel inside the same viewport.
- Changes are CSS-only and do not alter TUS, KOMİTE, scoring, card state, or AI logic.
