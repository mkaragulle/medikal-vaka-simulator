KlinikIQ V375 build test

Commands executed:
1. npm install --prefer-offline --no-audit --no-fund --progress=false --loglevel=warn
2. npm run build

Result:
- npm install completed successfully.
- Vite production build completed successfully.
- No syntax/import/build-breaking error was found.

Known non-blocking warning:
- Vite still reports large chunks for case-bank and GlossaryTooltip.
- This is expected because medical case and glossary data were intentionally preserved and not shortened.
