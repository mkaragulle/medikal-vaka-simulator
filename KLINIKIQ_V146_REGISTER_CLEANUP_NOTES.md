# KlinikIQ v146 — Register Screen Cleanup

- Register screen was simplified so it no longer feels cramped.
- The demo button, remember-me row, forgot-password action and bottom demo note are now shown only on the login screen.
- Register screen now keeps Google sign-up, the mode switcher, the required account fields and the primary submit action.
- Register-specific spacing overrides were added to keep the panel centered, cleaner and single-screen friendly.
- Existing orbital animation and dark-theme overrides were preserved.
