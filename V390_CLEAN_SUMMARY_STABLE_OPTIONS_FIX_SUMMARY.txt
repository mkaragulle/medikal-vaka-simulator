KlinikIQ V390 — Clean Summary Cards + Stable Answer Options Fix

Source base: KlinikIQ_V389_BIG_DATA_SMOOTHNESS_OPTIMIZED.zip

Changes:
1. Reworked the clinical patient summary presentation cards:
   - PROFİL, BAŞVURU and HASTA ÖYKÜSÜ cards now use a cleaner, calmer surface.
   - Removed noisy shadow/clip/paint effects that could erase top/bottom borders.
   - Added stable real borders, simpler spacing, and softer icon containers.
   - Updated summary icons in CasePlayer.jsx:
     * Profile: UserPlus
     * Presentation: HeartPulse
     * History: BookOpen
     * Main summary icon: Stethoscope
     * Risk: ShieldCheck
     * Clues: Target

2. Stabilized answer option cards:
   - Removed hidden ::before/::after overlay layers from answer options.
   - Disabled clip-path, mask, transform, paint containment and overflow clipping on answer option cards.
   - Rebuilt hover/selected/correct/wrong states using real borders and internal outline only.
   - This prevents the reported issue where option card top/bottom borders disappear on hover.

3. Build:
   - npm run build completed successfully.
   - Existing large chunk warnings remain; they are warnings, not build errors.
