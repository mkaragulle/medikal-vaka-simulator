# KlinikIQ — Global Topbar Alignment V111

## Applied changes
- Added a global topbar alignment patch so every text/icon pair is vertically centered inside its own capsule.
- Forced the product switch, context switch, and action chips to use consistent height, `box-sizing`, `align-items:center`, and no transform offset.
- Fixed the red **Zor** tab so the inner red pill is optically centered inside the white segmented capsule.
- Normalized SVG icon sizing and text line-height inside the topbar.
- Preserved responsive behavior for narrower screens.

## File changed
- `src/index.css`
